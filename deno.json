{
  "workspace": [
    "./packages/*",
    "./apps/*"
  ],
  "nodeModulesDir": "auto",
  "tasks": {
    "clean": "deno clean && rm -rf ./node_modules/",
    "fmt": "deno fmt",
    "lint": "deno lint",
    "test": "deno test -A",
    "check": "deno task fmt && deno task lint && deno check && deno task test",
    "dev": "cd apps/demo && deno task dev",
    "build": "cd apps/demo && deno task build",
    "start": "cd apps/demo && deno task start"
  },
  "fmt": {
    "lineWidth": 100,
    "semiColons": true,
    "singleQuote": false
  },
  "lint": {
    "rules": {
      "tags": [
        "recommended"
      ]
    }
  }
}
